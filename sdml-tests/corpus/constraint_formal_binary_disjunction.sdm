;;; Constraint, formal binary disjunction

module Example is

  structure Outer is

    inner -> string is
      assert not_empty is
        inner = "hi" or inner_sym = "bye"
      end
    end

    inner_sym -> string is
      assert not_empty is
        inner = "hi" ∨ inner_sym = "bye"
      end
    end

  end

end

