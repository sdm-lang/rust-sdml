module example is

  structure Example is
    default -> string
    one -> {1} string
    two -> {2} string
    zero_or_one -> {0..1} string
    one_or_more -> {1..} string
    seq_ordered -> {ordered 0..} string
    seq_unordered -> {unordered 0..} string
    seq_unique -> {unique 0..} string
    seq_nonunique -> {nonunique 0..} string
    seq_nonempty_set -> {unordered unique 1..} string
  end

end
