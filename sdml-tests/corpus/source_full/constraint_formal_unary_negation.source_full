module corpus_test_constraint is

  structure Outer is
    inner -> sdml:string is
      assert not_empty is
        not self.is_empty()
      end
    end
    inner_sym -> sdml:string is
      assert not_empty is
        not self.is_empty()
      end
    end
  end

end
