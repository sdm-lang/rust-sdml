module example_typeclass_defs is

  class Any (T) is
    def has_member(v -> T) -> sdml:boolean :=
      is_product_type(v)
    def is_entity(v -> T) -> sdml:boolean is
      @skos:prefLabel = "is an entity"
    end
    def is_structure(v -> T) -> sdml:boolean
    def new() -> T
  end

end
